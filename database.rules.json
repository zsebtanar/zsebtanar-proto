{
  "rules": {
    // admin can read and write everything
    ".read": "root.child('users/'+auth.uid+'/role').val() === 1000",
    ".write": "root.child('users/'+auth.uid+'/role').val() === 1000",

    "users": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "auth != null && auth.uid == $uid"
      }
    },

    "classifications": {
      ".read": true
    },

    "exercise": {
      "private": {
        "$exerciseId": {
          ".read": "root.child('users/'+auth.uid+'/role').val() > 0",
          ".write": false
        }
      },
      "public": {
        "$exerciseId": {
          ".read": true
        }
      }
    },
    "feedback": {
      ".write": "!data.exists() || root.child('users/'+auth.uid+'/role').val() === 1000",
      "$feedbackId": {
        "description": {
          ".validate": "newData.isString() && newData.val().length < 1024"
        }
      }
    },
    "storage": {
      ".write": "!data.exists() || root.child('users/'+auth.uid+'/role').val() === 1000",
      ".read": true
    }
  }
}
