import React from 'react'
import {connect} from 'react-redux'

const mapStateToProps = (state) => ({})

export default connect(mapStateToProps)(function Overlay(props) {
  return (
    <div>
    </div>
  )
})