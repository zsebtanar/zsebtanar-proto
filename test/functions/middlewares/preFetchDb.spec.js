// jest.mock('firebase-functions', () => require('../../../functions/mocks/firebase-functions'))
// jest.mock('firebase-admin', () => require('../../../functions/mocks/firebase-admin').default())

// const preFetchDB = require('../../../functions/src/middlewares/preFetchDB').default

describe('preFetchDB', () => {
  it.only('should work', () => {
    expect(true).toBe(true)
  })
})
