jest.mock('express')
import { process } from '../../../functions/src/exercise/getNextHint'

describe('https getNextHint endpoint', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })
})
